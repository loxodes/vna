The PRU initialization code, makefile, and headers are from following example: 
Beaglebone-PRU-RPMsg-HelloWorld
https://github.com/RoSchmi/Beaglebone-PRU-RPMsg-HelloWorld
Code in this directory is licensed GPL v3.0

sudo make clean        // (to delete the old executable files)

sudo make install -B   // (to compile and install the programs for PRU0 and PRU1)

sudo make -B           // (to compile the Linux user space program)

sudo ./adc_to_socket   // (to start the Linux user space program)
